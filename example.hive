
[export:yes]
(i64[] input)main(i32? res)
{
    (10)range[on:local](var tmp);
    res <- ?tmp + ?tmp + ?tmp;
}

(i32 n)range(i32| res)
{
    i32 i;
    i <- 1;
    while i < n
    {
        res <- i;
        i <- i + 1;
    }
}
