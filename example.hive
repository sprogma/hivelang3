@x64

[export:yes]
[entry:yes]
[integer64:yes]
(i64[] args)main(i64? resultCode)
{
    i8[] msg;
    msg <- new i8[]"Hello world!\n";
    ?(1, msg, ?msg)write@dll(*);
    resultCode <- 0;
}

[dllimport:libc.so]
[dllimport.entry:write]
(i32 file, i8[] text, i64 size)write(i32? result);
