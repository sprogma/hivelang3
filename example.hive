@x64

[export:yes]
[entry:yes]
(i64[] args)main(i32? resultCode)
{
    i32[]@loc x;
    x <- new i32[]@loc(50);
    i32 i;
    i <- 2;
    while i <= 30
    {
        x[i] <- x[i - 1] + x[i - 2];
        i <- i + 1;
    }
    resultCode <- x[30];
}
