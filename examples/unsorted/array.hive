@x64

[export:yes]
[entry:yes]
[integer64:yes]
(i64[] args)main(i64? resultCode)
{
    i64[]@loc x;
    x <- new i64[]@loc(32);
    i64 i;
    x[0] <- 0;
    x[1] <- 1;
    i <- 2;
    while i < ?x
    {
        x[i] <- x[i - 1] + x[i - 2];
        i <- i + 1;
    }
    resultCode <- x[?x - 1];
}
